version: 2
updates:
  # Keep the vendored pnpm installer (src/pnpm/vendor, a git submodule of https://github.com/pnpm/get.pnpm.io) up to date.
  - package-ecosystem: "gitsubmodule"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "pnpm"
